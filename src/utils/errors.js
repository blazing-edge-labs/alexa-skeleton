// these are errors for logs

const errors = {
  invalidIntentSlots: 'Invalid intent slots',
  intentNotAllowed: 'That intent is not allowed at this time',

  middlewareNeedsToBeAnArray: 'Middleware needs to be an array!',

  sayNotProvided: 'No say text has been provided!'
}

module.exports = errors
